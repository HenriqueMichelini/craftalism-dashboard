Use this prompt to start a new AI/code-agent session in this repository.

First read:

1. `AGENTS.md`
2. `docs/index.md`
3. `docs/context-policy.md`
4. `docs/workflows/index.md`

Then select the appropriate workflow from `docs/workflows/`.

For card-based implementation work, use:

```text
docs/workflows/implement-card.md
docs/features/<feature-name>/cards/<CARD-ID>.md
```

Replace `<feature-name>` and `<CARD-ID>` with the selected feature and card.

Do not scan unrelated docs or source files. Do not implement if the selected workflow or card fails readiness checks.
